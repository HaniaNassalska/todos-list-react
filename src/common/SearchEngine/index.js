import { StyledForm, Input } from "./styled";

const SearchEngine = () => (
  <StyledForm>
    <Input
      placeholder="Filtruj zadania"
      autoFocus
    />
  </StyledForm>
);

export default SearchEngine;
