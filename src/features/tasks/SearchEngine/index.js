import { StyledForm, Input } from "./styled";

const SearchEngine = () => (
  <StyledForm>
    <Input
      placeholder="Filtruj"
      autoFocus
    />
  </StyledForm>
);

export default SearchEngine;
